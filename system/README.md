# 包裝產能系統（開發中）

目標形態：專人每天／每週在網頁介面 key in 每批包裝資料 → 永久存進資料庫 →
月初自動檢核完整性 → 一鍵產生儀表板 → 一鍵下載當月 PDF 簡報。

目前的 `../index.html`（上傳 xlsx 即出結果）維持不動，兩者並行；
系統上線後，儀表板改為直接讀資料庫，xlsx 版本仍可作為離線備援。

---

## 階段規劃

| 階段 | 內容 | 狀態 |
|---|---|---|
| **0** | 資料庫建置、權限、歷史資料匯入 | ✅ 完成 |
| **備份** | Google Drive 每月自動匯出（Apps Script） | ✅ 程式完成，待安裝 |
| 1 | 資料輸入介面（登入、批次輸入、即時產能檢核） | ✅ 完成 |
| 2 | 補登進度月曆、月度完整性檢核 | 未開始 |
| 3 | 儀表板改讀資料庫、月報 PDF | 未開始 |
| 4 | 人均效率分析（以 2026-08 為基準起點） | 未開始 |

---

## 階段 0 成果

**Supabase 專案**：`smart-equipment-patrol`（`vnncuksjvdsxwveomoww`，ap-northeast-1）
使用獨立的 `packing` schema，與既有巡檢系統的 `public` schema 完全隔離。

### 資料表

| 資料表 | 用途 | 筆數 |
|---|---|---|
| `packing.skus` | 品項主檔（品名、單件重量、容器型式） | 110 |
| `packing.batches` | 批次記錄（主表） | 1,593 |
| `packing.daily_status` | 標記「當日確實無包裝作業」 | 0 |
| `packing.members` | 使用者角色 | 1 |
| `packing.audit_log` | 稽核記錄（任何異動自動留痕，不可竄改） | — |

### 兩個關鍵設計

**1. 工時是「算出來的」，不是「填進去的」**

`batches.hours` 是 generated column，永遠等於 `(結束 − 起始)`，跨午夜自動加 24 小時。
原始 Excel 裡有 3 筆人工填的工時與起訖時間對不上；改成資料庫自動推算後，
這類錯誤在結構上就不可能再發生。

**2. 「沒生產」與「忘了登記」要分得開**

`daily_status` 讓輸入專員按一個「本日無包裝作業」按鈕。
沒有批次記錄、又沒有這個標記的日期，才算漏登 —— 這是階段 2 月度檢核的判定基礎。

### 權限（RLS）

| 角色 | 讀 | 寫 |
|---|---|---|
| `operator` 輸入專員 | 全部 | 只能新增，且**只能修改當月**資料 |
| `manager` 主管 | 全部（含稽核記錄） | 不限月份 |
| `viewer` 檢視者 | 全部 | 不可寫 |

月份限制用台灣時區判定（`packing.today_tw()`），避免 UTC 造成月初月底誤判。
稽核記錄已 `revoke` 掉所有寫入權限，只有觸發器能寫。

---

## 歷史資料匯入結果

| 指標 | 數值 |
|---|---|
| 期間 | 2025-10-01 ～ 2026-08-31（11 個月） |
| 批次 | 1,593 |
| 品項 | 110 |
| 總包裝量 | 2,185,914 |
| 總工時 | 2,811.6 |
| 整體產能 | 777 /工時 |
| 有人數的批次 | 154（全部集中在 2026-08） |

### 與現行儀表板數字的差異（已查明，非匯入錯誤）

儀表板內建的是舊檔（到 8/26），資料庫用的是最新檔（到 8/31）：

| 差異 | 筆數 | 說明 |
|---|---|---|
| 8/27～8/31 新增批次 | 22 | 舊檔尚未涵蓋的月底五天，含新品項 `Z2001` |
| `D0310` 5/16 兩筆補上瓶數 | 2 | 舊檔該欄空白故未納入，新檔已補 409 / 508 |
| `D0530` 8/6 瓶數更正 | 1 | 319 → 1,500 |

合計 +22 批、+45,554 瓶、+56.9 工時；整體產能兩者同為 **777**。

---

## 檔案

```
system/supabase/migrations/   資料庫結構，依序執行即可重建
system/backup/                每月自動備份到 Google Drive 的程式與安裝說明
system/test_entry.py          輸入介面的自動化測試
entry.html                    輸入介面（部署在 GitHub Pages）
index.html                    儀表板
```

**版控裡只放「結構」，不放「資料」。** 歷史批次與品項主檔放在
[Google Drive 備份資料夾](https://drive.google.com/drive/folders/1FXHhPneNgMc4H11ZzrFbnhF1h-B-v9vq)，
由每月自動備份維護。

要重建資料庫時：先依序跑 `migrations/` 建好結構，再把 Drive 上的
`packing_skus_*.csv` 與 `packing_batches_*.csv` 匯入。
批次 CSV **不含工時** —— 工時由資料庫從起訖時間推算，匯入也不會被採用。

---

## 登入方式

使用者只輸入**帳號**，不輸入信箱：

```
帳號：admin000
密碼：（自行設定）
```

Supabase Auth 底層一定要用信箱格式，所以前端會自動把 `admin000` 補成
`admin000@packing.local` 再送出。**使用者從頭到尾看不到信箱**，也不用記。
日後新增輸入專員時同理：帳號 `packer01`，後面自動補 `@packing.local`。

| 帳號 | 角色 | 用途 |
|---|---|---|
| `admin000` | manager 主管 | Jackie 哥，不限月份可改 |
| `backup` | viewer 唯讀 | 每月備份程式專用，只能讀 |

---

## 備份

**位置**：Google Drive →「AI系統資料庫」→
[包裝產能資料庫備份](https://drive.google.com/drive/folders/1FXHhPneNgMc4H11ZzrFbnhF1h-B-v9vq)

**方式**：Google Apps Script，每月 10 號凌晨 3 點自動匯出三張表的 CSV。
跑在 Google 的機器上，不需要開電腦或伺服器，零成本。
安裝步驟見 `system/backup/README.md`。

備份用唯讀帳號讀取，密碼存在 Apps Script 的指令碼屬性，不進版控。
另附 `verifyLatestBackup()` 可比對「資料庫筆數 vs 備份筆數」，
用來抓「備份有跑但檔案是空的」這種最難發現的失效。

---

## 階段 1 成果：輸入介面 `entry.html`

網址（合併到 main 後）：
`https://jackielin666.github.io/packaging-capacity-dashboard/entry.html`

和儀表板一樣是單一 HTML 檔，沒有打包工具、沒有框架、不依賴外部套件網站
—— 工廠內網擋掉 CDN 也照樣能開。

### 現場怎麼用

| 動作 | 做法 |
|---|---|
| 打時間 | 直接打 `0800`，離開欄位自動變 `08:00`。也接受 `800`、`8`、`8:00` |
| 選品項 | 打品號前兩碼即出現候選，常做的排在前面 |
| 換欄位 | Enter 往下一格；打到最後一格自動開新的一列，手不用離開鍵盤 |
| 從 Excel 搬 | 整塊複製貼上（品項／起始／結束／瓶數／人數），一次填好多列 |
| 當天沒做 | 按「標記本日無包裝作業」 |
| 找漏登的日子 | 按「跳到待補的日期」，自動找回最早一個沒處理的工作日 |

工時和產能是邊打邊算的，不用填也填不了。

### 三個刻意的設計

**1. 異常當場抓，不等月底**

每打完一批就比對該品項的歷史水準，低於平均 ×0.7 或高於 ×2.5 就標出來。
這種數字九成是瓶數或時間打錯 —— 當下抓到，比月底對帳再回頭查省事得多。
批次數不足 10 批的品項不示警，樣本太少沒有判斷力。

**2. 新增和修改的權限分開**

| | 輸入專員 | 主管 |
|---|---|---|
| 新增（含補登過去月份） | ✅ 任何日期 | ✅ |
| 修改／刪除既有資料 | 只限當月 | ✅ 不限 |

補登要能往回填，但歷史數字不該被無聲改掉。畫面上會直接說明，
不會讓現場以為是壞了。

**3. 今天不算漏登**

當天還沒過完就標紅字，每天早上打開都在示警，久了會被當成雜訊直接忽略。
只有「今天以前」的工作日才會被算成待補。

### 測試

`system/test_entry.py` 用 Playwright 攔截網路請求模擬資料庫，
把登入到儲存的完整流程跑一遍，共 32 項檢查：時間格式轉換、工時計算、
異常門檻、半填欄位的阻擋、送出的欄位與型別、月曆狀態、Excel 貼上。

```
python3 system/test_entry.py
```

改過 `entry.html` 之後跑一次，確認沒有改壞。

---

## 待辦（需要 Jackie 哥操作）

1. **開放 `packing` schema 給 Data API**
   Supabase 後台 → Project Settings → API → Exposed schemas，把 `packing` 加進去。
   不做這步，前端與備份程式都讀不到任何資料。

2. **安裝備份程式**
   照 `system/backup/README.md` 做，約 5 分鐘，只需做一次。

3. **設定 `admin000` 的密碼**
   目前是開發用的臨時密碼，真實生產資料進來前必須更換。

---

## 已知限制

- Drive 的上層資料夾「AI系統資料庫」擁有者是別的帳號（`rbsjt2013@gmail.com`），
  子資料夾「包裝產能資料庫備份」擁有者才是 Jackie 哥。
  上層若被停用或收回共用，備份會連同一起失聯 —— 長期建議把備份資料夾搬到
  Jackie 哥自己的「我的雲端硬碟」底下。
- Supabase 免費方案沒有自動備份，上述 Apps Script 是唯一的第二份副本，
  加上 GitHub 上的 `system/supabase/seed/` 共兩份。
